export const host = "http://localhost:8080/";

export const clientsEndpoint = host + "clients";

export const loginEndpoint = host + "auth/login";
export const employesEndpoint = host + "api/employes"; 
